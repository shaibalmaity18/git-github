
USER@DESKTOP-6MB4BNC MINGW64 /d/git-demo/project1 (master)
$ history
    1  git version
   19  cd d:
   20  cd git-demo/
   21  ll
   22  mkdir project1
   23  cd project1/
   24  git status
   25  git init
   26  git status
   27  git config --global user.name"shaibalmaity18"
   28  git status
   29  git config --global user.name
   30  git config --global user.name "shaibalmaity18"
   31  git config --global user.name
   32  git config --global user.email
   33  git config --global user.email "shaibalmaity18@gmail.com"
   34  git config --global user.email
   35  git config user.name
   36  git config user.email
   37  clear
   38  history

